---
title: Installation
description: Install the APX CLI, start the daemon, and verify your setup.
sidebar:
  order: 2
---

import { Steps, Tabs, TabItem, Aside } from '@astrojs/starlight/components';
import Screenshot from '../../../components/Screenshot.astro';
import Terminal from '../../../components/Terminal.astro';

APX ships as a single npm package that provides three binaries: `apx` (CLI), `apx-daemon`
(the local server), and `apx-mcp` (the MCP server bridge).

## Requirements

- **Node.js 20+** (the package engines field allows 18+, but 20+ is recommended).
- macOS, Linux, or WSL. Some surfaces (Desktop, Voice) have extra OS-level dependencies covered on
  their own pages.

## Install

<Tabs syncKey="pkg">
	<TabItem label="npm">
		```bash
		npm install -g @agentprojectcontext/apx
		```
	</TabItem>
	<TabItem label="pnpm">
		```bash
		pnpm add -g @agentprojectcontext/apx
		```
	</TabItem>
</Tabs>

<Aside type="tip">
	The daemon starts automatically on your first `apx` call — you don't need to launch it by hand.
</Aside>

## Verify

<Steps>

1. Check the CLI is on your PATH:

   ```bash
   apx --version
   ```

2. Run any command to boot the daemon, then confirm it's healthy:

   ```bash
   apx status
   ```

   <Terminal command="apx status" caption="apx status showing the daemon running and its port">
   {`  Daemon
     ✓ running   port 7430   v1.38.0   uptime 3m

   Super-agent
     ✓ enabled
       model           anthropic:claude-sonnet-4-5

   Engines
     ✓ Anthropic     ← active
     ✓ OpenAI
     ✓ Gemini
     ✓ Ollama        http://localhost:11434

   Projects
     #1  First Project    5 agents   ~/code/first-project
     #2  Acme Store        4 agents   ~/code/acme-store
     #3  Data Pipeline     3 agents   ~/code/data-pipeline`}
   </Terminal>

</Steps>

## What gets created

The first run creates the local runtime directory:

```text
~/.apx/
├── config.json        ← engines, providers, global settings
├── identity.json      ← the assistant's user-facing name (default "APX")
└── projects/          ← per-project runtime state (sessions, messages, caches)
```

None of this is committed — it's local to your machine. See
[Configuration](/apx/docs/concepts/configuration/) for the full reference.

## Next

Head to the [Quick start](/apx/docs/start/quick-start/) to initialize a project and run your first agent.
