---
title: Routines
description: Routines scheduladas de APX — kinds, gramática de schedule, pipeline pre/post y todos los subcomandos CLI.
sidebar:
  order: 3
---

import { Aside, Steps } from '@astrojs/starlight/components';
import Screenshot from '../../../../components/Screenshot.astro';

Una **routine** es una tarea APX schedulada. El daemon corre un tick del scheduler cada 5 segundos
y dispara cualquier routine que esté pendiente. Cada routine tiene un `kind`, un `schedule`, un
blob JSON `spec` opcional, y arrays opcionales de hooks shell (`pre_commands` / `post_commands`).

## Kinds

| Kind | ¿LLM? | ¿Herramientas? | Descripción |
|---|---|---|---|
| `heartbeat` | No | — | Loguea un marcador. Útil como ping "still alive". |
| `shell` | No | — | Corre un comando shell. Stdout capturado. |
| `exec_agent` | Sí | Ninguna | Carga el prompt de un agente de proyecto, envía `spec.prompt`, devuelve texto plano. Una sola llamada. |
| `super_agent` | Sí | Todas | Corre el agente APX por defecto con el registro completo de herramientas. Loop multi-iteración. |
| `telegram` | No | — | Envía un `spec.text` hardcodeado via el plugin de Telegram. |

**Regla de elección:**

- ¿Necesitás texto de un modelo, sin llamadas a herramientas? → `exec_agent`.
- ¿Necesitás orquestación (escribir archivos, llamar MCPs, crear tareas, enviar Telegram dinámico)? → `super_agent`.
- ¿Shell puro sin LLM? → `shell`.
- ¿Mensaje de Telegram fijo en un schedule? → `telegram`.

## Gramática de schedule

| Formato | Ejemplos | Notas |
|---|---|---|
| `every:<N><unit>` | `every:30s`, `every:5m`, `every:24h`, `every:7d` | Lo más común. |
| `once:<iso-8601>` | `once:2026-12-01T08:00:00Z` | Corre una vez, luego se deshabilita. |
| Expresión cron | `*/5 * * * *`, `0 8 * * *` | Cron estándar de 5 campos. |

## `apx routine add`

```bash
apx routine add <name> \
  --kind <kind> \
  --schedule <schedule> \
  [--spec '<json>'] \
  [--pre-commands 'cmd1,cmd2'] \
  [--post-commands 'cmd'] \
  [--skip-prompt-on signal|pre_failure|pre_success|always|never] \
  [--permission-mode total|automatico|permiso] \
  [--allowed-tools tool1,tool2] \
  [--project <name|id|path>]
```

<Aside type="caution">
  Siempre pasá `--project`. Sin él, la routine cae en el workspace scratch por defecto (id=0),
  no en tu proyecto real.
</Aside>

### `--spec`

JSON de configuración específico del kind:

| Kind | Claves requeridas | Opcionales |
|---|---|---|
| `exec_agent` | `prompt` | `agent` (slug, default `default`) |
| `super_agent` | `prompt` | — |
| `shell` | `cmd` | — |
| `telegram` | `text` | — |
| `heartbeat` | _(ninguna)_ | — |

### `--pre-commands` y `--post-commands`

Comandos shell separados por coma. Forman un pipeline alrededor de la llamada al LLM:

<Steps>

1. **`pre_commands`** corren secuencialmente. Su stdout combinado está disponible como:
   - `{{pre_output}}` — sustituido en `spec.prompt` antes de la llamada al LLM.
   - `$APX_PRE_OUTPUT` — variable de entorno para `post_commands`.
   - `$APX_PRE_OUTPUT_FILE` — ruta a un archivo temporal con la salida completa (para payloads grandes).

2. El **handler del kind** corre. Su resultado de texto se expone como `$APX_LLM_OUTPUT`.

3. **`post_commands`** corren secuencialmente con `$APX_LLM_OUTPUT`, `$APX_PRE_OUTPUT` y
   `$APX_STATUS` en el entorno.

</Steps>

### `--skip-prompt-on`

Controla qué pasa cuando `pre_commands` salen con código no-cero:

| Valor | Comportamiento |
|---|---|
| `signal` _(default)_ | Omite el LLM solo en SIGINT/SIGTERM; exit no-cero igual corre el LLM. |
| `pre_failure` | Omite LLM + post en cualquier exit no-cero. |
| `pre_success` | Omite LLM + post a menos que todos los pre commands salgan con 0. Igual que `pre_failure` en la mayoría de los casos. |
| `always` | Siempre omite el LLM — útil para pipelines puros pre→post. |
| `never` | Siempre corre el LLM, incluso si los pre commands crashean. |

### `--permission-mode`

Sobrescribe el `super_agent.permission_mode` global para esta routine:
`total` | `automatico` | `permiso`.

## Otros subcomandos

```bash
apx routine list [--project <name|id|path>]
apx routine get  <name> [--project <name|id|path>]
apx routine history <name> [--project <name|id|path>]
apx routine run  <name> [--project <name|id|path>]    # forzar disparo ahora
apx routine enable  <name> [--project <name|id|path>]
apx routine disable <name> [--project <name|id|path>]
apx routine remove  <name> [--project <name|id|path>]
```

<Screenshot
  surface="terminal"
  caption="apx routine list — nombre, kind, schedule, habilitada, próximo y último run"
  hint="Run: apx routine list --project myapp" />

## Ejemplos

### Texto plano + entrega por Telegram

```bash
apx routine add clima-diario \
  --project myapp \
  --kind exec_agent \
  --schedule "every:24h" \
  --spec '{"agent":"default","prompt":"El clima es {{pre_output}}. Una frase amigable, sin saludos."}' \
  --pre-commands "curl -s 'https://wttr.in/BuenosAires?format=%t+%C+viento+%w'" \
  --post-commands 'apx telegram send "$APX_LLM_OUTPUT"'
```

### Super-agent con herramientas, schedule matutino

```bash
apx routine add standup-matutino \
  --project myapp \
  --kind super_agent \
  --schedule "0 9 * * *" \
  --spec '{"prompt":"Listá las tareas abiertas en todos los proyectos y mandame un resumen corto por Telegram."}' \
  --permission-mode automatico
```

### Shell puro, sin LLM

```bash
apx routine add backup-db \
  --project myapp \
  --kind shell \
  --schedule "every:24h" \
  --spec '{"cmd":"pg_dump mydb > /backups/mydb-$(date +%F).sql"}'
```

### Run único en el futuro

```bash
apx routine add deploy-prod \
  --project myapp \
  --kind super_agent \
  --schedule "once:2026-07-01T10:00:00Z" \
  --spec '{"prompt":"Corré el checklist de deploy a producción y reportá el estado."}'
```

## El gotcha del double-reply

<Aside type="caution">
  Al usar el kind `super_agent` con un `post_commands` que llama a `apx telegram send`,
  se envían **dos** mensajes de Telegram: uno desde la propia llamada a la herramienta
  `send_telegram` del agente, otro desde `post_commands`. El runner suprime automáticamente
  `send_telegram` cuando detecta `apx telegram send` en `post_commands`, pero la solución
  limpia es usar `exec_agent`: el modelo escribe el texto, `post_commands` lo envía. Un mensaje.
</Aside>

```bash
# ✗ Frágil — depende de la heurística de supresión
--kind super_agent \
--spec '{"prompt":"El clima es {{pre_output}}. Mandalo por Telegram."}' \
--post-commands 'apx telegram send "$APX_LLM_OUTPUT"'

# ✓ Limpio — el modelo escribe el texto, el shell lo entrega
--kind exec_agent \
--spec '{"prompt":"El clima es {{pre_output}}. Una frase amigable, sin saludos."}' \
--post-commands 'apx telegram send "$APX_LLM_OUTPUT"'
```

## Debugging

```bash
apx routine history <name> --project myapp   # últimos N runs con estado y salida
apx log -f                                   # tail del log unificado del daemon
apx messages tail --channel routine -n 20    # últimos 20 mensajes del canal routine
```

Una routine que "no envía nada" la mayoría de las veces significa: `enabled: false`, `next_run_at`
está en el futuro, o el LLM devolvió texto vacío. Revisá `apx routine history` primero — el campo
`result.text` muestra exactamente qué produjo el modelo.

## Siguiente

- [Tasks](/apx/es/capabilities/tasks/) — lista TODO por proyecto que las routines pueden leer y escribir.
- [Super-agent](/apx/es/capabilities/super-agent/) — el modo bajo el cual corren las routines `super_agent`.
- [Configuración](/apx/es/concepts/configuration/) — fallback de modelo global y configuración de permisos.
