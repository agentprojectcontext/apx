org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true

# The app version people read, and the one the release tag carries. Bump it by
# hand when the app changes; CI supplies the versionCode (see app/build.gradle).
apxAppVersion=0.2.0
