org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true

# The app version people read, and the one the release tag carries. NOT edited
# by hand: .github/workflows/android.yml works out the next one from the commit
# subjects and writes it back here, the way semantic-release does package.json.
# CI supplies the versionCode separately (see app/build.gradle).
apxAppVersion=0.4.1
