{
  "name": "weekly-review",
  "kind": "exec_agent",
  "schedule": "{{weekly_schedule}}",
  "enabled_by_default": true,
  "spec": {
    "agent": "{{orchestrator_agent}}",
    "prompt": "WEEKLY RUN — the review.\n\nOne question only: what moved this week, and what is now at risk?\n\nThe state of this company today is below. It is not instruction, it is what is true right now; if anything in your own definition contradicts it, the state wins.\n\n{{pre_output}}\n\nA source marked `unavailable` IS A FINDING: say it in one clause and carry on with the others. Never fill a gap with an estimate.\n\nAlso bring your tasks and commitments, scoped to THIS project, including overdue ones.\n\nLook for: commitments overdue or about to be, work parked for days, promises made to a person, and anything with no owner. Never the inventory — what moved and went fine does not need a line.\n\nWORK FIRST, THEN THE BRIEF. Every finding that needs somebody to act becomes a task BEFORE you write: `create_task` assigned to the agent whose area it is (their slug in `agent`), or `comment_task` on the existing task mentioning them (@slug) — that hands it over, and they take it up in their own turn. Do not ask anyone for anything in the brief and do not message agents: the brief says what you assigned and to whom, and keeps as a recommendation only what needs the OWNER (a decision, money, a person outside). A finding with no task and no owner decision is inventory — drop it.\n\nAt most 15 lines.\n\nFormat: one bullet per finding, `- [scope] what happened or what is at risk → what you recommend`. Prioritised, no preamble, no sign-off. If nothing deserves an interruption, answer the single line `NO_MESSAGE — <why>`; a quiet week is a result. To escalate, make `SEVERITY: blocker` the first line — that crosses quiet hours and the weekly cap, so use it when something is broken or money is being lost today."
  },
  "deliver_to": [
    "{{deliver_to}}"
  ],
  "pre_commands": [
    "apx company context --ritual weekly"
  ],
  "post_commands": [
    "apx company handoff --ritual weekly"
  ]
}
